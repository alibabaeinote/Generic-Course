# راهنمای مشارکت

قرارداد کامل همکاری در [`docs/GOVERNANCE.md`](docs/GOVERNANCE.md) است. این فایل فقط مسیر سریع اجرای آن را نشان می‌دهد.

## پیش از شروع

1. `AGENTS.md` و `docs/GOVERNANCE.md` را بخوان.
2. `PRD.md`، `TECH-STACK.md` و `design-system/style-guide.md` را برای scope کار بررسی کن.
3. وضعیت branch و تغییرات local را بررسی کن؛ تغییرات عامل دیگر را دست‌نخورده نگه دار.
4. در branch/worktree جدا کار کن.

## هنگام تغییر

- برای تصمیم جدید یا تغییر scope، اول سند مرجع را اصلاح کن.
- از معماری یا token موازی نساز.
- محتوای فارسی و RTL را با متن واقعی و عرض موبایل بررسی کن.
- تغییرات را کوچک، قابل بازبینی و تک‌موضوعی نگه دار.

## پیش از push

- diff را بازبینی کن؛
- تست‌های مرتبط و build را اجرا کن؛
- اسناد تحت‌تأثیر را بررسی کن؛
- branch پایه را همگام کن؛
- در commit و گزارش، وضعیت دقیق را بنویس.

برای v1، معیار انتشار در [`docs/PUBLISHING.md`](docs/PUBLISHING.md) است. انتشار زنده بدون live verification انجام‌شده تلقی نمی‌شود.
