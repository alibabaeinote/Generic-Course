# دیزاین سیستم — استایل‌گاید (جهت هـ · Graphic Blue v4)

> **وضعیت: فعال.** این سند دقیقاً همان پیش‌نیازی است که `step-0.md` برای شروع دوباره خواسته بود:
> «انتخاب جهت بصری، سپس استایل‌گاید». جهت بصری انتخاب شده — **جهت هـ، Graphic Blue** — و این سند
> مقدارهای واقعیِ همان جهت را مستند می‌کند، نه فرضی یا آماده‌به‌تغییر.
>
> **منبع حقیقت بصری:** `prototype/landing-v4-graphic.html`. هر مقدار این سند از همان فایل استخراج شده.
> اگر فایل عوض شد و این سند نه، **فایل درست است، سند را به‌روزرسانی کن.** این سند دنبال‌کنندهٔ کد است، نه برعکس.
>
> این سند سه‌تای دیگر را جایگزین نمی‌کند، تکمیل می‌کند: `content/voice.md` (لحن)، `content/values.md`
> (مواضع محتوایی)، `design-system/step-0.md` (معماری توکن‌ها و نقشهٔ راه — بایگانی، اما بخش قواعد RTL و
> ماتریس حالت‌هایش هنوز معتبر و همین‌جا هم ارجاع داده می‌شود).

---

## ۱. پرینسیپل — چهار ارزش پشت هر تصمیم بصری

هر عنصر تازه‌ای که به سایت اضافه می‌شود باید حداقل با یکی از این چهار اصل توضیح داده شود. اگر نشد،
آن عنصر تزیین است، نه دیزاین سیستم.

| اصل | یعنی چه | کجای کد همین الان دیده می‌شود |
|---|---|---|
| **همراهی** | کنار مخاطب ایستادن، نه بالای سرش | لحن اول‌شخص جمع در همهٔ متن‌ها (`voice.md` قاعدهٔ ۵)؛ آیکون‌ها و بج‌ها هم‌قد و کنار متن‌اند، نه به‌عنوان تزیین بالای آن |
| **خودمانی** | صمیمی، بدون فرمالیته‌ی اداری | گوشه‌های کاملاً گرد (`--pill: 999px`) روی هر دکمه و تگ؛ اسپات‌لایت رنگی زیر موس در هیرو (بازیگوشی کنترل‌شده، نه یک المان رسمی)؛ فعل محاوره‌ای در UI هم رعایت می‌شود («ببینیم چه مسیری داره» نه «مشاهدهٔ مسیر») |
| **گرم** | رنگ زنده، نه خاکستریِ کورپوریت | پالت اصلی نارنجی‌قرمز (`--accent`) + زرد (`--yellow`) + سبز (`--green`) در کنار آبی؛ کارت‌ها روی زمینهٔ زرد بدون border سرد هستند (`.s-yellow .card`)؛ هیچ‌جای متن اصلی از خاکستریِ بی‌روح استفاده نشده — حتی متن ثانویه یک `--ink-2` گرم است، نه `#888` خنثی |
| **پیکان توسعه** | همیشه رو به جلو، رو به رشد | شمارندهٔ آماری که از ۰ بالا می‌رود؛ نوار مسیر (Journey Track) که از `۰۰` به `۱۱` عدد را زیاد می‌کند؛ شمارهٔ هر سکشن در باند کناری (`.split__num`) صعودی و همیشه رو به جلوست |

---

## ۲. لحن و کپی

جزئیات کامل در `docs/content/voice.md` — این‌جا فقط خلاصهٔ عملیاتی برای کسی که کامپوننت می‌سازد:

- فعل محاوره‌ای («می‌شه»، «داره»)، اسم مفهومی محکم (نه «باحال»، «معیار»).
- اول‌شخص جمع همه‌جا («یاد می‌گیریم»)، به‌جز FAQ و بخش دربارهٔ مدرس (اول‌شخص فردی درست است چون گوینده مشخص است).
- صفت تبلیغاتی ممنوع؛ ادعا یا مدرک دارد یا حذف می‌شود.
- روی UI هم همین قانون حاکم است: متن دکمه فعل است («ببینیم چه مسیری داره») نه اسم مصدر («مشاهدهٔ مسیر»).
- اعداد همیشه انگلیسی‌اند (۹+ → 9+) — چه در متن آماری چه در UI (تگ Chapter، شمارهٔ سکشن، FAQ) — و با همان فونت لاتین (`--lat`) که اعداد آماری دارند، تا در کل صفحه یک زبان عددی باشد.

---

## ۳. رنگ — نقش، نه فقط مقدار

### توکن‌های خام (لایهٔ Primitive، دقیقاً از `:root`)

```css
--yellow:#FFD400;   --paper:#F5F4EF;    --accent:#E23F1C;   --green:#16A34A;
--white:#FFFFFF;    --ink:rgb(14 17 32); --ink-2:#3A3D45;
--blue:#1024CF;      --blue-2:#4A63FF;   --blue-ink:#0A1580;
--line:rgba(8,9,12,.16);  --line-2:rgba(8,9,12,.3);
--on-dark:#F3F5FA;   --line-d:rgba(243,245,250,.22);
```

### نقش‌ها (لایهٔ Semantic — این ستون را در تصمیم‌گیری استفاده کن، نه اسم متغیر خام)

| نقش | توکن | کجا |
|---|---|---|
| زمینهٔ خنثی | `--white` / `--paper` | سکشن‌های سفید؛ `--paper` فقط برای کارت‌های روی زمینهٔ سفید (یک پله تیره‌تر از خودِ زمینه) |
| زمینهٔ برند/تاکید | `--yellow` | زمینهٔ کامل سکشن مسئله و بخش‌های اثبات؛ باند شمارهٔ سکشن‌های روشن (`--band-light`) |
| زمینهٔ معکوس | `--ink` | سکشن‌های تیره (فلسفه، مهارت‌ها، ثبت‌نام)، فوتر، باند شمارهٔ پیش‌فرض هر سکشن |
| زمینهٔ کنش | `--blue` | فقط برای سکشن‌های ویژه (به‌ندرت به‌کار رفته)؛ نقش اصلی‌اش کنش است نه زمینه (پایین را ببین) |
| متن اصلی | `--ink` | روی زمینهٔ روشن |
| متن ثانویه | `--ink-2` | لید، پاراگراف بدنه، برچسب |
| متن روی معکوس | `--on-dark` | روی `--ink` / `--blue` |
| خط جداکننده روشن | `--line` / `--line-2` | مرز کارت، تگ، آکاردئون روی زمینهٔ روشن |
| خط جداکننده تیره | `--line-d` | همان نقش‌ها، روی زمینهٔ `--ink` / `--blue` |
| **رنگ کنش** | `--blue` | تنها رنگی که «کلیک کن» می‌گوید: دکمهٔ CTA، عدد آمار، لینک هاورشده، حالت باز آکاردئون (`aria-expanded="true"`) |
| لهجهٔ داغ ۱ | `--accent` (نارنجی‌قرمز) | بج‌های شمارهٔ پیش‌فرض، حرف تاکیدی در پول‌کوت روی زمینهٔ زرد |
| لهجهٔ داغ ۲ | `--green` | معادل بصری `--accent` با وزن یکسان — برای تنوع در بج‌ها و گرافیک وقتی همه‌چیز نارنجی‌قرمز شود یکنواخت به نظر می‌رسد |

**قاعده:** `--accent` و `--green` هم‌وزن‌اند و جای‌گزین هم — هیچ‌کدام سلسله‌مراتب بالاتری از دیگری ندارد.
`--blue` تنها رنگی است که معنی «قابل کلیک» می‌دهد؛ هرگز به‌عنوان رنگ تزیینی صرف استفاده نشود (این دقیقاً
همان اشتباهی بود که در بازبینی زنده اصلاح شد: متن تاکیدی روی زمینهٔ زرد از آبی به مشکی تغییر کرد چون
آبی آن‌جا کنش را القا می‌کرد بدون این‌که قابل کلیک باشد).

### ریتم رنگی صفحه

هر سکشن پس‌زمینهٔ خودش را دارد، نه یک پس‌زمینهٔ یکنواخت با کارت‌های رنگی رویش. ترتیب رایج:
سفید → زرد → سفید → تیره (ink) → سفید → ... فوتر همیشه `--ink`.

---

## ۴. تایپوگرافی

```css
--fa:  "Vazirmatn", system-ui, sans-serif;                    /* فارسی و هر متن اصلی */
--lat: "Space Grotesk", "Vazirmatn", system-ui, sans-serif;   /* عدد، اصطلاح لاتین، تگ Chapter */
--mono:"Space Mono", ui-monospace, monospace;                  /* برچسب‌های کوچک uppercase: PROBLEM, CHAPTER 01 */
```

| نقش | قاعده | نمونه |
|---|---|---|
| h1 | `clamp(2.1rem, 6.1vw, 4.1rem)` وزن ۹۰۰ | تیتر Hero |
| h2 | `clamp(1.85rem, 5.2vw, 3.5rem)`، حداکثر ۱۶ کاراکتر عرض | تیتر سکشن |
| h3 | `clamp(1.05rem, 2.2vw, 1.3rem)` وزن ۸۰۰ | تیتر کارت |
| لید (`.lede`) | `clamp(1.02rem, 2.2vw, 1.28rem)`، رنگ `--ink-2`، حداکثر ۴۶ کاراکتر | پاراگراف زیر تیتر |
| بدنه (`.copy`, `p`) | ۱۷px پایه، `line-height:1.9`، حداکثر ۵۰ کاراکتر | متن توضیحی |
| برچسب مونو | `.7rem–.76rem` (کف فعلی سیستم — قبلاً `.62rem–.68rem` بود، به دلیل ناخوانایی در موبایل بزرگ شد)، `letter-spacing:.1em–.24em`، UPPERCASE | شمارهٔ سکشن، Chapter، DT در specs — **فقط برای متن لاتین** |
| پیل هویتی (`.tag--solid`) | فارسی، `--fa`، `.92rem` وزن ۷۰۰، بدون `letter-spacing`، بدون uppercase | «اردوی آنلاین...» در هیرو، «● اردو فعاله» |
| عدد شاخص (`.n`, `.split__num`) | فونت `--lat`، وزن ۳۰۰ (باریک عمداً)، `tabular-nums` | آمار، شمارهٔ باند سکشن |
| مقدار specs (`.specs dd`) | `clamp(1.15rem, 2.6vw, 1.4rem)` وزن ۸۰۰ | مقدار هر مشخصه (مثلاً «۲ ماه»، «آنلاین») — عمداً بزرگ‌تر از بدنه چون تنها محتوای اصلی کارت است |

**قاعدهٔ فارسی (از تجربهٔ همین پروژه):** هرگز متن فارسی را `vertical-rl` نکن — اتصال حروف کاملاً می‌شکند.
اگر یک برچسب باید عمودی بنشیند (مثل `.split__band .lab` در دسکتاپ)، آن برچسب باید **لاتین** باشد
(`SKILLS`, `PROOF`) با `text-orientation:sideways`؛ فارسی همیشه افقی می‌ماند.

اصطلاح لاتین وسط جملهٔ فارسی همیشه با `.lat` بپیچد (`unicode-bidi: isolate`)، وگرنه جهت متن به‌هم می‌ریزد.

**قاعدهٔ دوم (هم از تجربهٔ همین پروژه):** روی متن فارسی هرگز `letter-spacing` نذار و هرگز `--mono`
نکن. هر دوتا فرض می‌کنن حروف مستقل از هم رسم می‌شن؛ فارسی حروفش به‌هم می‌چسبه، پس `letter-spacing`
اتصال رو می‌شکنه و باعث می‌شه متن تکه‌تکه و ناخوانا دیده بشه (این دقیقاً همون باگی بود که تگ هویتی
هیرو رو خراب کرده بود — روی `.tag` پایه `letter-spacing:.14em` و `font-family:var(--mono)` بود، در
حالی که محتواش فارسی بود؛ چون `--mono` هیچ فallback فارسی نداره، به فونت مونوی سیستم برمی‌گشت و
هم‌زمان با tracking باز، اتصال حروف رو کامل از بین می‌برد). این قاعده مثل قاعدهٔ بالا (بدون
`vertical-rl`) غیرقابل‌مذاکره است: `letter-spacing` و `--mono` فقط برای متن **لاتین** مجازند.

---

## ۵. فاصله، شبکه و شکل

```css
--pad:  clamp(1.1rem, 4vw, 3.5rem);   /* پدینگ افقی هر .wrap */
--pill: 999px;                          /* شعاع کامل — همهٔ دکمه‌ها، تگ‌ها، بج‌ها */
container: 84rem (max-width .wrap)
```

| شعاع گوشه | مقدار | کجا |
|---|---|---|
| کامل (`--pill`) | 999px | دکمه، تگ، بج، pill نوار مسیر |
| بزرگ | 26px | کارت اصلی (`.card`, `.fig`, `.bf`) |
| متوسط | 20–22px | آکاردئون، specs، loop card |

**نقاط شکست — وضعیت فعلی (نه ایده‌آل):** کد از چند نقطهٔ شکستِ موردی به‌جای یک مقیاس واحد استفاده
می‌کند: `52rem`(832px)، `56rem`(896px، اصلی‌ترین — مرز موبایل/دسکتاپ برای split و track)، `60rem`(960px)،
`64rem`(1024px). این با مقیاس پیشنهادیِ `step-0.md`(`sm480·md768·lg1024·xl1280`) یکی نیست. **بدهی شناخته‌شده:**
قبل از اضافه‌کردن گرید جدید، یکی از این دو مقیاس را انتخاب و یکدست کن؛ فعلاً به همین ad-hoc ادامه نده.

**Grid gap:** بین `.7rem` تا `.8rem` برای اکثر گریدهای کارت؛ فاصلهٔ بین بلوک‌های بزرگ (تیتر تا گرید،
گرید تا pull-quote) با `clamp(2rem,5vw,3rem)` تا `clamp(2.5rem,6vw,3.6rem)`.

---

## ۶. حرکت

```css
--e:  cubic-bezier(.16,1,.3,1);   /* اصلی — ease-out نرم، برای تقریباً همه‌چیز */
--e2: cubic-bezier(.25,1,.5,1);   /* برای opacity در .rv، کمی متفاوت از --e */
```

| الگو | مدت | کجا |
|---|---|---|
| Reveal روی اسکرول (`.rv`) | `.7s` | فید + جابه‌جایی ۲۰px به بالا، با `IntersectionObserver` |
| Line-reveal تیتر (`.ln > i`) | `1s` | متن از داخل ماسک `overflow:hidden` بالا می‌آید — فقط برای h1/h2/pull-quote |
| هاور کارت | `.45s` | `translateY(-5px)` + تیره‌شدن border |
| هاور دکمه (fill-wipe) | `.5s`–`.55s` | رنگ از پایین دکمه بالا می‌آید (`transform:translateY(101%→0)` روی `::after`/`::before`) — هم در `.cta` هم در `.bf` |
| باز/بسته آکاردئون | `.55s` | ترفند `grid-template-rows: 0fr → 1fr` (بدون اندازه‌گیری JS) |
| شمارندهٔ آمار | `1.3s` | ease-out درجه‌چهار دستی در JS، نه CSS |

**قاعده:** `prefers-reduced-motion: reduce` کل سیستم حرکت را خاموش می‌کند (مدت‌ها به `.001ms`، حالت نهایی
مستقیم نشان داده می‌شود). این چک همیشه هم در CSS (`@media`) هم در JS (`matchMedia`) تکرار می‌شود — یک‌بار
در بالای `<script>` به متغیر `R` می‌ریزد و همه‌جا از همان استفاده می‌کنند.

---

## ۷. گرافیک

| عنصر | چیست | قاعده |
|---|---|---|
| هالهٔ محو (`.haze`) | دایرهٔ رنگی با `blur(70px)` و opacity کم، پشت محتوا | فقط برای عمق‌بخشی به زمینه؛ هرگز محتوا رویش سوار نمی‌شود بدون z-index بالاتر |
| گرید نقطه‌ای (`.dotgrid`) | فقط در Hero؛ دو لایه — یک بستر همیشه‌کم‌رنگ + یک لایهٔ پررنگ‌تر که با ماسک شعاعی دنبال موس می‌رود | لایهٔ دوم رنگش با موقعیت موس بین سبز/آبی/نارنجی‌قرمز/زرد (تیره‌شده برای خوانایی) نرم عوض می‌شود؛ روی لمسی و بدون‌موس فقط لایهٔ اول + یک اسپات‌لایت ثابت بالای هیرو دیده می‌شود |
| آیکون | SVG اینلاین، `viewBox 0 0 24 24`، `stroke:currentColor`، `stroke-width:2`، گوشهٔ گرد (`round`/`round`)، اندازهٔ نمایش ۱۶px (به‌جز specs: ۲۴px / `1.5rem`) | فلش ↖ (خروج/CTA)، فلش ↗ (درون کارت)، تیک (تناسب/Fit)، به‌علاوهٔ شش آیکون مفهومی مخصوص specs سرفصل (ساعت، وای‌فای/آنلاین، پخش، مداد، بابل گفت‌وگو، دو نفر — بخش ۸.۲). همه دست‌ساز و هم‌قرارداد، چون منبع بیرونی (Streamline) توسط سیاست شبکه بلاک است. طبق قاعدهٔ RTL در `step-0.md`، جهت فلش با بافت (ورود به محتوا / خروج از آن) عوض می‌شود، نه رنگ یا شکلش |
| کارت‌های آماری (`.fig`) | عدد بزرگ (`--lat` وزن ۳۰۰) + برچسب فارسی کوچک | تنها جایی که رنگ `--blue` روی متن آمار (نه دکمه) به‌کار رفته — چون این عدد هم «قابل اثبات» است، هم بخشی از هویت کنش‌محور صفحه |

---

## ۸. اجزا (کامپوننت‌ها)

### ۸.۱ اتم‌ها

| کامپوننت | کلاس | حالت‌ها |
|---|---|---|
| دکمهٔ اصلی | `.cta` | default، hover (fill-wipe مشکی؛ بج فلش رنگ عوض می‌کند، کج نمی‌شود) |
| دکمهٔ ثانویه | `.ghost` | default (outline)، hover (پر می‌شود — روی `.hero` همیشه پس‌زمینهٔ سفید توپر دارد تا زیر گرید نقطه‌ای گم نشود) |
| تگ/برچسب | `.tag`, `.tag--solid`, `.tag--blue` | یک نسخهٔ outline (پیش‌فرض) و دو نسخهٔ پرشده |
| بج دایره‌ای | `.badge`, `.badge--fill`, `.badge--blue`, `.badge--green` | outline رنگی → روی هاورِ کارت پدر، پر و آبی می‌شود (بدون چرخش — قاعدهٔ عمدی: هیچ‌چیز داخل دایره روی هاور کج نمی‌شود) |
| پیل مسیر | `.track button` | default، hover، `.on` (فعالِ اسکرول‌اسپای) |

### ۸.۲ ارگانیسم‌ها

| کامپوننت | کلاس | نکتهٔ کلیدی |
|---|---|---|
| کارت | `.card` | رنگ زمینه به پس‌زمینهٔ سکشن بستگی دارد: سفید روی زرد، `--paper` روی سفید، شیشه‌ای تیره روی ink — **کارت هرگز رنگ ثابت خودش را ندارد** |
| کارت آماری | `.fig` | همیشه سفید با بردر، مستقل از سکشن |
| کارت بریف (با پرشدن رنگی) | `.bf`, `.bf--x` | تنها کارتی که کل زمینه‌اش روی هاور عوض می‌شود (آبی)؛ `--x` نسخهٔ ازقبل‌تیرهٔ آن روی سکشن ink است |
| سرستون سکشن (Split header) | `.split` / `.split__band` / `.split__band--light` / `.split__main` | باند کناری (شماره + برچسب لاتین) در دسکتاپ ستون عمودی سمت راست، در موبایل نوار افقی بالا؛ `--light` فقط وقتی به‌کار می‌رود که پس‌زمینهٔ خودِ سکشن تیره باشد (تا کنتراست باند حفظ شود) |
| آکاردئون | `.acc` / `.acc__item` / `.acc__btn` / `.acc__p` | یک قاب بیرونی واحد + خط جداکنندهٔ نازک بین ردیف‌ها (نه کارت جدا برای هر ردیف)؛ **پیش‌فرض بسته** — هیچ ردیفی از قبل باز نمی‌آید |
| Hero | `.hero` | تنها جایی با گرید نقطه‌ای تعاملی و هالهٔ رنگی؛ محتوا وسط‌چین و عمودی |
| فوتر | `footer` | همیشه `--ink`؛ لینک‌ها روی هاور زرد می‌شوند |

---

## ۹. الگوهای اینتراکشن

| الگو | رفتار | پیاده‌سازی |
|---|---|---|
| نوار مسیر (Journey Track) | از `section[data-label]` (۸ تای اول) به‌صورت پویا ساخته می‌شود؛ کلیک=اسکرول نرم، اسکرول=هایلایت خودکار | `IntersectionObserver` برای اسکرول‌اسپای، `scrollIntoView` برای کلیک |
| شمارندهٔ آماری | از ۰ تا مقدار هدف، فقط وقتی وارد ویوپورت شود | `IntersectionObserver` یک‌بارمصرف + `requestAnimationFrame` |
| آکاردئون | باز/بسته با `aria-expanded` روی دکمه؛ ارتفاع با ترفند grid، نه JS-measured height | هندلر کلیک ساده روی `.acc__btn` |
| اسپات‌لایت رنگی هیرو | فقط با موس واقعی (`hover:hover and pointer:fine`)؛ رنگ به ربع صفحه بستگی دارد، با ease نرم به‌جای پرش | rAF loop که رنگ فعلی را به‌سمت رنگ ربع هدف `lerp` می‌کند — تا رنگ‌های میانی «قهوه‌ای/کثیف» نشوند |
| Reveal روی اسکرول | متن و کارت‌ها با تأخیر پلکانی (`transitionDelay`) بر اساس ترتیب خواهر و برادر ظاهر می‌شوند | یک `IntersectionObserver` مشترک برای `.rv` و `.ln` |

**قاعدهٔ طلایی همهٔ الگوهای بالا:** هرکدام باید در نبودِ JS/انیمیشن هم کاملاً قابل استفاده باشند (حالت
نهایی، نه خالی) — این با کف دسترسی‌پذیریِ `step-0.md` («`prefers-reduced-motion` کل سیستم حرکت را
خاموش می‌کند») یکی است و همین‌جا هم رعایت شده.

---

## ۱۰. نام‌گذاری — قاعدهٔ واقعیِ این پروژه

کد فعلی از BEM سختگیرانه پیروی نمی‌کند، اما یک قاعدهٔ ثابت و قابل‌پیش‌بینی دارد — همین را ادامه بده:

- **کلاس بلوک کوتاه و بدون پیشوند:** `.card`, `.badge`, `.acc`, `.fig`, `.cta`
- **فرزند با جداکنندهٔ دو زیرخط:** `.acc__item`, `.acc__btn`, `.split__band`, `.fig .n`(بدون زیرخط چون تک‌کلمه‌ای و در بافت `.fig` بی‌ابهام است)
- **حالت با `--`:** `.badge--fill`, `.badge--green`, `.tag--solid`, `.split__band--light`
- **رنگ سکشن با پیشوند `s-`:** `.s-yellow`, `.s-white`, `.s-ink`, `.s-blue` — این کلاس‌ها روی خودِ `<section>` می‌نشینند و فرزندان (`.card`, `.badge`, `.tag`, ...) با سلکتور فرزندی (`.s-ink .card`) خودشان را با پس‌زمینه هماهنگ می‌کنند. **نتیجه:** یک کامپوننت هرگز رنگ خودش را هارد-کد نمی‌کند، رنگ را از سکشن اطرافش می‌گیرد.
- **توکن CSS:** `--<نقش>` تخت، بدون دسته‌بندی سه‌لایه‌ای که `step-0.md` پیشنهاد داده بود (`--btn-primary-bg` و مشابه آن هنوز پیاده نشده). این یک بدهیِ شناخته‌شده است، نه یک تصمیم نهایی: تا وقتی تعداد سکشن‌ها و کامپوننت‌ها به نقطه‌ای برسد که تغییر یک نقش رنگی نیاز به گشتن در چند جا داشته باشد، همین ساختار تخت کافی است.
- **شناسهٔ سکشن:** `id="s-<نام>"` + `data-label="<برچسب فارسی کوتاه>"` — این دومی است که موتور نوار مسیر و اسکرول‌اسپای را می‌سازد؛ اضافه‌کردن یک سکشن جدید بدون `data-label` یعنی از نوار مسیر جا می‌ماند.

---

## ۱۱. وقتی سکشن یا صفحهٔ جدید می‌سازی — چک‌لیست سریع

۱. پس‌زمینه یکی از چهارتاست: `s-white` / `s-yellow` / `s-ink` / `s-blue`. رنگ تصادفی جدید نساز.
۲. اگر سرستون شماره‌دار می‌خواهد، از الگوی `.split` استفاده کن؛ `--light` را فقط روی زمینهٔ تیره بزن.
۳. کارت‌ها را با متغیرهای نقش رنگ کن، نه مقدار مستقیم — پس‌زمینهٔ کارت به‌طور خودکار با `.s-*` عوض می‌شود.
۴. عنصر تعاملیِ دایره‌ای/بج را روی هاور نچرخان — رنگ/پرشدن کافی است. (پیش‌تر چرخش ۴۵ درجه به‌عنوان نماد «پیکان توسعه» امتحان شد؛ عمداً حذف شد چون محتوای داخل دایره را کج و کج‌خوان می‌کرد.)
۵. هر متن جدید را از صافیِ هفت‌قاعدهٔ `voice.md` رد کن، بعد commit کن.
۶. `id="s-..."` و `data-label="..."` را فراموش نکن.
۷. با `prefers-reduced-motion` و در عرض ۳۲۰px تست کن (کف دسترسی‌پذیری `step-0.md`، بخش ۷).
