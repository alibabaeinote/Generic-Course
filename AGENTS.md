# قرارداد همکاری عامل‌ها

این ریپو یک پروژهٔ واحد است و Codex و Claude Code هم‌تیمی‌های آن هستند. این فایل قرارداد اجرایی مشترک است؛ جزئیات کامل در [`docs/GOVERNANCE.md`](docs/GOVERNANCE.md) قرار دارد.

## الزامات غیرقابل مذاکره

1. پیش از هر تغییر، `docs/GOVERNANCE.md` و وضعیت Git را بخوان.
2. برای محصول، `docs/PRD.md` منبع حقیقت است؛ برای ظاهر و کامپوننت‌ها، `docs/design-system/style-guide.md`؛ برای معماری فنی، `docs/TECH-STACK.md`؛ و برای تصمیم‌های ثبت‌شده، `docs/DECISIONS.md`.
3. تغییر تصمیم، معماری یا قرارداد طراحی باید در همان commit در سند مرجع ثبت شود. چت، issue و توضیح commit به‌تنهایی منبع حقیقت نیستند.
4. هر عامل در branch/worktree جدا کار کند؛ روی checkout مشترک یا فایل‌های تغییرکردهٔ عامل دیگر کار نکن.
5. پیش از شروع، آخرین وضعیت branch پایه را بگیر. پیش از push، تغییرات پایه را merge/rebase کن و تست‌ها را اجرا کن.
6. تغییرات عامل دیگر را overwrite، reset یا حذف نکن. در تعارض، کار را متوقف کن و تعارض را بر اساس منابع حقیقت حل کن.
7. ادعای «تکمیل»، «push» یا «انتشار» فقط با شواهد همان مرحله مجاز است.

## ترتیب منابع حقیقت

در تعارض، سند تخصصی‌تر بر سند عمومی‌تر مقدم است؛ اما تعارض باید با اصلاح اسناد حل شود:

`DECISIONS.md` (تصمیم نهایی) → `PRD.md` / `TECH-STACK.md` / `style-guide.md` (قرارداد تخصصی) → کد و prototype → README و متن‌های توضیحی

هر تغییر واقعی باید همهٔ منابع تحت‌تأثیر را در همان تغییر هماهنگ کند.
